a = 5
b = 8
print("a AND b:", a and b)
print("a OR b:", a or b)
print("NOT a:", not a)
print("a > b:", a > b)
print("a == b:", a == b)
print("a >= b:", a >= b)
print("a <= b:", a <= b)
print("a < b:", a < b)

# algorithm

# 1.  Start.
# 2.  Initialize variables: `a = 5`, `b = 8`.
# 3.  Calculate & Print Logical Operations:
#      `a and b` (Result is `8`)
#      `a or b` (Result is `5`)
#      `not a` (Result is `False`)
# 4.  Calculate & Print Relational Comparisons:
#      `a > b` (Result is `False`)
#      `a == b` (Result is `False`)
#      `a >= b` (Result is `False`)
#      `a <= b` (Result is `True`)
#      `a < b` (Result is `True`)
# 5.  End.